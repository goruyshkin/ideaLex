= form_for @ethnoidiogloss do |f|
  - if @ethnoidiogloss.errors.any?
    #error_explanation
      %h2= "#{pluralize(@ethnoidiogloss.errors.count, "error")} prohibited this ethnoidiogloss from being saved:"
      %ul
        - @ethnoidiogloss.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :word
    = f.text_field :word
  .field
    = f.label :description
    = f.text_area :description
  .field
    = f.label :status
    = f.number_field :status
  .field
    = f.label :author
    = f.text_field :author
  .field
    = f.label :user
    = f.text_field :user
  .field
    = f.label :book
    = f.text_field :book
  .actions
    = f.submit 'Save'
